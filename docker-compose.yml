version: "2"
services:

    wordpress:
        build: .
        ports:
            - "8080:80"
        links:
            - mysql

    mysql:
        image: mysql:5.5
        environment:
            - MYSQL_DATABASE=wordpress
            - MYSQL_USER=wordpress
            - MYSQL_PASSWORD=secret
            - MYSQL_ALLOW_EMPTY_PASSWORD=yes


